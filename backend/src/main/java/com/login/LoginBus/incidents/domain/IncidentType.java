package com.login.LoginBus.incidents.domain;

/**
 * Describes the primary type of an incident.
 */
public enum IncidentType {
    ACCIDENT,
    VEHICLE_BREAKDOWN,
    DELAY,
    ROAD_OBSTRUCTION,
    MEDICAL,
    CHILD_BEHAVIOR,
    OTHER
}