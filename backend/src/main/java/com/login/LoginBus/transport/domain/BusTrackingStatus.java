package com.login.LoginBus.transport.domain;

/**
 * Bus tracking status enum for domain layer.
 */
public enum BusTrackingStatus {
    NOT_IN_ROUTE,
    PICKING_UP_CHILDREN,
    GOING_TO_SCHOOL,
    AT_SCHOOL,
    DROPPING_OFF_CHILDREN
}
