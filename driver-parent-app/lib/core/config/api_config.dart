class ApiConfig {
  /// Base URL for the LoginBus Spring Boot backend.
  /// Running on port 8080 (default). Update the IP to match your dev machine.
  static const String baseUrl = 'http://localhost:8080';
}